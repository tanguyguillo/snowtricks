{% extends 'base.html.twig' %}

{% set route = '' %}
{% set routeTitre = '' %}

{# titre #}
{% block title %}
{% set route = app.request.attributes.get('_route') %}

{% if route == 'tricks_details' %}
{% set routeTitre = 'details' %}
{% endif %}

{% if route == 'tricks_modification' %}
{% set routeTitre = 'modifications' %}
{% endif %}

{% if is_granted('IS_AUTHENTICATED_REMEMBERED') and app.request.attributes.get('_route') == "tricks_modification" %}
{% set routeTitre = 'modifications' %}
{% endif %}

Snowtricks - {{ routeTitre }} - {{ trick.title }}
{% endblock %}




{% block main %}

<br><br><br>
<div class="container text-center">
    <br>
    <br>
    <br>

    <pre>
        {{ app.request.attributes.get('_route') }}
        {{ route }}
    </pre>




    {# {% if is_granted('IS_AUTHENTICATED_REMEMBERED') and route%}
    <p> test....</p>
    {% endif %} #}


    <div class="row justify-content-center">
        <div class="col-lg-12">
            <div class="modifications">
                <div class="rounded shadow p-3 mb-5 bg-white">

                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') and app.request.attributes.get('_route') ==
                    "tricks_modification" %}

                    <span>
                        <a href="#tricksDelete" class="float-right ml-1">
                            <i class="bi bi-trash-fill iconTricks"></i>
                        </a>
                        <a href="/tricks/modification/{{ trick.slug }}" class="float-right ml-1" alt="fff">
                            <i class="bi bi-pencil-fill iconTricks"></i>
                        </a>
                    </span>

                    {% endif %}

                    {# main picture #}
                    <img class="bd-placeholder-img bd-placeholder-img-lg img-fluid " width="100%" height="150"
                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/{{ trick.picture }}"
                        alt="{{ trick.title }}">

                    <div class="caption post-content">

                        <h1>{{ trick.title }}
                            {# for instance.... #}
                            {# <a href="#tricksDelete" class="float-right ml-1">
                                <i class="bi bi-trash-fill iconTricks"></i>
                            </a> #}
                            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                            <a href="/tricks/modification/{{ trick.slug }}" class="float-right ml-1">
                                <i class="bi bi-pencil-fill iconTricks"></i>
                            </a>
                            {% endif %}
                        </h1>
                    </div>
                </div>


                {####### additionnal picture #########}
                <div class="row justify-content-center">

                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}} 1" />

                                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                    <div class="card-body">
                                        {# Only if connected #}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                    </div>
                                    {% endif %}

                                </div>
                            </div>
                        </div>
                    </div>




                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}}" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}}" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <br>

                    {####### additionnal video #########}
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 1" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- video -->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 2" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- video -->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 3" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

                {####### information #########}
                <div class="card-body">
                    <ul class="list-inline">
                        <li class="list-inline-item">
                            created by : {{ Author.username }}
                        </li class="list-inline-item"> |
                        <li class="list-inline-item">
                            created at : {{ trick.createdAt|date('d-m-Y') }} {#(<small>updated : </small>) #}
                        </li> |
                        <li class="list-inline-item">
                            Groupe : {{ trick.category }}

                            {# </li> | #}
                        {# <li class="list-inline-item">
                            <a href="#comments-block">13 commentaires</a>
                        </li> #}
                    </ul>
                </div>

                {######### description ##########}
                <div class="text-left content text-white"> {{ trick.content }}</div>
            </div>
        </div>
    </div>
</div>
<div>
    <br>
    <br>
    <br>
    <div class="container text-center">
        <div class="row">
            <div class="col">
                Groupe
            </div>

            <div class="col">
            </div>

            <div class="col">
                <button class=" btn btn-danger" id="delete" type="submit">delete</button>
            </div>
            <div class="col">
                <button class=" btn btn-info" type="submit">save</button>

            </div>
        </div>
    </div>


</div>
<br>
<br>
<br>
<br>
<br>
<br>
{% endblock %}