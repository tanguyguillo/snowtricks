{% extends 'base.html.twig' %}

{% block title %}Snowtricks - details - {{ trick.title }} {% endblock %}

{% block main %}


<br><br><br>
<div class="container text-center">
    <br>
    <br>
    <br>

    {% set route = '' %}

    {% if app.request.attributes.get('_route') == 'tricks_details' %}
        <p> route : {{app.request.attributes.get('_route')}}</p>

        {# {% set env = {{app.request.attributes.get('_route')}} }' %} #}

    {% endif %}

    {# <pre>
        {{ dump(env) }}
     </pre> #}

    <div class="row justify-content-center">

        {# {% for flash_error in app.flashes('') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %} #}

        <div class="col-lg-12">

            <div class="modification">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                {% endif %}

                <div class="rounded shadow p-3 mb-5 bg-white">

                    <span>

                        <a href="#tricksDelete" class="float-right ml-1">
                            <i class="bi bi-trash-fill iconTricks"></i>
                        </a>
                        <a href="/tricks/modification/{{ trick.slug }}" 
                            class="float-right ml-1"
                            alt="fff">
                            <i class="bi bi-pencil-fill iconTricks"></i>
                        </a>

                    </span>

                    {# main picture #}
                    <img class="bd-placeholder-img bd-placeholder-img-lg img-fluid " width="100%" height="150"
                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/{{ trick.picture }}"
                        alt="{{ trick.title }}">

                    <div class="caption post-content">

                        <h1>{{ trick.title }}
                            {# for instance.... #}
                            {# <a href="#tricksDelete" class="float-right ml-1">
                                <i class="bi bi-trash-fill iconTricks"></i>
                            </a> #}
                            <a href="/tricks/modification/{{ trick.slug }}" class="float-right ml-1">
                                <i class="bi bi-pencil-fill iconTricks"></i>
                            </a>
                        </h1>
                    </div>
                </div>


                {####### additionnal picture #########}
                <div class="row justify-content-center">

                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid zz_image"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}} 1" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    


                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}}" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}}" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <br>

                    {####### additionnal video #########}
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 1" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- video -->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 2" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- video -->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 3" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>



                {####### information #########}
                <div class="card-body">
                    <ul class="list-inline">
                        <li class="list-inline-item">
                            created by : {{ Author.username }}
                        </li class="list-inline-item"> |
                        <li class="list-inline-item">
                            created at : {{ trick.createdAt|date('d-m-Y') }} {#(<small>updated : </small>) #}
                        </li> |
                        <li class="list-inline-item">
                            Groupe : {{ trick.category }}

                            {# </li> | #}
                        {# <li class="list-inline-item">
                            <a href="#comments-block">13 commentaires</a>
                        </li> #}
                    </ul>
                </div>

                {######### description ##########}
                <div class="text-left content text-white"> {{ trick.content }}</div>


            </div>





        </div>
    </div>
</div>
<br>
<br>
<br>
<br>
<br>
<br>
{% endblock %}