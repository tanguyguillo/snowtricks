{% extends 'base.html.twig' %}

{% block title %}Snowtricks - Modification - {{ trick.title }} {% endblock %}

{% block main %}
<br><br><br>
<div class="container text-center">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <br>
            <br>
            <br>
            <br>
            <h2 class="tricks-modal-title text-secondary mb-0 text-center"> {{ trick.title }}</h2>
            <br><br>

                {# {% for flash_error in app.flashes('verify_email_error') %}
                <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
                {% endfor %} #}
                <p>blabla Modification </p>
                

                <br> 
                <br>

        </div>
    </div>
    {% endblock %}