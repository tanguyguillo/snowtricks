{% extends 'base.html.twig' %}

{% block title %}Snowtricks - Modification - {{ trick.title }} {% endblock %}

{% block main %}
<br><br><br>
<div class="container text-center">
    <br>
    <br>
    <br>

    <div class="row justify-content-center">

        {% for flash_error in app.flashes('') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}

        <div class="col-lg-12">

            <div class="modification">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                {% endif %}

                <div>
                    <img class="bd-placeholder-img bd-placeholder-img-lg img-fluid rounded" width="100%" height="150"
                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/{{ trick.picture }}"
                        alt="{{ trick.title }}">

                    <div class="caption post-content">
                        <h1>{{ trick.title }}
                            {# for instance.... #}
                            <a href="#tricksDelete" class="float-right ml-1">
                                <i class="bi bi-trash-fill iconTricks"></i>
                            </a>
                            <a href="/tricks/modification/{{ trick.slug }}" class="float-right ml-1">
                                <i class="bi bi-pencil-fill iconTricks"></i>
                            </a>
                        </h1>
                    </div>
                </div>


                {####### additionnal picture #########}
                <div class="row justify-content-center">

                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}} 1" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <hr>

                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}}" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- tricks Items-->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-picture.jpg"
                                        alt="Additionnal picture {{ trick.title|striptags}}" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>


                    {####### additionnal video #########}
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 1" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- video -->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 2" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- video -->
                    <div class="col-sm-2">
                        <div class="mx-auto">
                            <div class="d-flex align-items-center justify-content-center h-100 w-100">
                                <div class="card">
                                    <img class="img-fluid"
                                        src="{{ app.request.getSchemeAndHttpHost()}}/assets/img/tricks/main-video.png"
                                        alt="Additionnal video {{ trick.title|striptags}} 3" />
                                    <div class="card-body">
                                        {# Only if connected #}
                                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                                        <span>
                                            <a href="#tricksDelete" class="float-center ml-1">
                                                <i class="bi bi-trash-fill iconTricks"></i>
                                            </a>
                                            <a href="/tricks/modification/{{ trick.slug }}" class="float-center ml-1">
                                                <i class="bi bi-pencil-fill iconTricks"></i>
                                            </a>
                                        </span>
                                        {% endif %}

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>



                {####### information #########}
                <div class="card-body">
                    <ul class="list-inline">
                        <li class="list-inline-item">
                            created by : {{ Author.username }}
                        </li> |
                        <li class="list-inline-item">
                            created at : {{ trick.createdAt|date('d-m-Y') }} {#(<small>updated : </small>) #}
                        </li> |
                        <li class="list-inline-item">
                            Groupe : {{ trick.category }}

                            {# </li> | #}
                        {# <li class="list-inline-item">
                            <a href="#comments-block">13 commentaires</a>
                        </li> #}
                    </ul>
                    <hr>
                </div>

                {######### description ##########}
                <div> {{ trick.content }}</div>


            </div>





        </div>
    </div>
</div>
<br>
<br>
<br>
{% endblock %}